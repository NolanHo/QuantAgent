services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    image: quantagent-api:local
    command: ["sh", "-c", "APP_ENV=${APP_ENV:-local} exec uvicorn quantagent.api.main:app --host 0.0.0.0 --port 8000"]
    ports:
      - "${API_BIND_HOST:-127.0.0.1}:${API_PORT:-8000}:8000"
    volumes:
      - ./apps/api:/app/apps/api
      - ./runtime:/app/runtime
    depends_on:
      db:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
        required: false

  worker:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    image: quantagent-api:local
    command: ["quantagent-worker"]
    volumes:
      - ./apps/worker:/app/apps/worker
      - ./runtime:/app/runtime
    depends_on:
      db:
        condition: service_healthy
      kafka:
        condition: service_healthy
        required: false
      migrate:
        condition: service_completed_successfully
        required: false

  scheduler:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    image: quantagent-api:local
    command: ["quantagent-scheduler"]
    volumes:
      - ./apps/scheduler:/app/apps/scheduler
      - ./runtime:/app/runtime
    depends_on:
      db:
        condition: service_healthy
      kafka:
        condition: service_healthy
        required: false
      migrate:
        condition: service_completed_successfully
        required: false

  migrate:
    image: quantagent-api:local
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    profiles:
      - migration
    working_dir: /app/packages/core
    command: ["quantagent-db", "upgrade"]
    environment:
      APP_ENV: ${APP_ENV:-local}
      DATABASE_URL: ${MIGRATION_DATABASE_URL:-postgresql+psycopg://quantagent:quantagent@db:5432/quantagent}
      RUNTIME_DIR: /app/runtime
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
    depends_on:
      db:
        condition: service_healthy

  db:
    image: postgres:17
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-quantagent}
      POSTGRES_USER: ${POSTGRES_USER:-quantagent}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-quantagent}
    ports:
      - "${DB_HOST:-127.0.0.1}:${DB_PORT:-15432}:5432"
    volumes:
      - quantagent-db:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -h 127.0.0.1 -p 5432 -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 10

  kafka:
    image: bitnami/kafka:4.0.0
    profiles:
      - kafka
    # 本地 Kafka profile 默认是易失运行时，不为 topic/offset 提供持久化卷。
    environment:
      KAFKA_CFG_NODE_ID: 0
      KAFKA_CFG_PROCESS_ROLES: controller,broker
      KAFKA_CFG_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_CFG_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093
      KAFKA_CFG_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      KAFKA_CFG_CONTROLLER_QUORUM_VOTERS: 0@kafka:9093
      KAFKA_CFG_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_CFG_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_CFG_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_CFG_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_CFG_NUM_PARTITIONS: 1
    healthcheck:
      test: ["CMD-SHELL", "kafka-topics.sh --bootstrap-server 127.0.0.1:9092 --list >/dev/null 2>&1"]
      interval: 10s
      timeout: 5s
      retries: 12

volumes:
  quantagent-db:
