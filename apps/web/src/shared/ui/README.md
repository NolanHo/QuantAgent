# shared/ui

该目录放跨业务域复用、无业务规则的基础展示组件。

当前负责：

- 页面区块卡片、区块标题、页面标题、事实列表和轻量标签。
- LinkButton 等不依赖具体业务 DTO、权限策略或请求状态的基础交互。

当前不负责：

- 事件、审批、插件、运行态等业务字段映射。
- API 响应、TanStack Query、权限判断、审批动作或运行态策略。
- 特定 feature 的页面编排和业务状态。
