# Route Mock Extension Point

Use this directory for Playwright route mock helpers and fixtures introduced by issue `#55`.

Keep page-level smoke tests in `../` and place reusable mock adapters or mock payload builders here when browser network mocking is implemented.
