[project]
name = "quantagent-plugin-sdk"
version = "0.1.0"
description = "QuantAgent plugin author SDK"
requires-python = ">=3.11"
dependencies = []

[tool.uv.build-backend]
module-root = "src"
module-name = "quantagent.plugin_sdk"

[build-system]
requires = ["uv_build>=0.11.14,<0.12"]
build-backend = "uv_build"
