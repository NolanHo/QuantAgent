{
  "type": "object",
  "additionalProperties": false,
  "required": ["webhook_url"],
  "properties": {
    "webhook_url": {
      "type": "string",
      "minLength": 1,
      "sensitive": true,
      "description": "Discord webhook URL used for notification.send."
    },
    "timeout_seconds": {
      "type": "number",
      "minimum": 0.1,
      "default": 5,
      "description": "Request timeout used for Discord webhook send requests."
    }
  }
}
