{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Jina Reader Source Plugin Config",
  "description": "Minimal public configuration for the official Jina Reader source plugin.",
  "type": "object",
  "properties": {
    "url": {
      "type": "string",
      "format": "uri",
      "description": "The web page URL that Jina Reader will fetch and parse."
    },
    "timeout_seconds": {
      "type": "integer",
      "minimum": 1,
      "maximum": 120,
      "default": 20,
      "description": "Maximum seconds to wait for the Jina Reader service to return a result."
    },
    "allow_external_reader": {
      "type": "boolean",
      "description": "If false, the platform has denied sending the URL to an external reader."
    }
  },
  "required": ["url"],
  "additionalProperties": false
}
